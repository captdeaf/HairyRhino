/* Listening host + port. */
var HOST = '0.0.0.0';
var PORT = 9999;

/* SHould we turn off RAD stuff? (jsload, etc) ? */
var PRODUCTION = false;

/* Directory containing all the sites. Relative to this dir. */
var SITES_DIR = '../sites';
